public class Code {

    /*
    List<String> result = new ArrayList<>();

for (String s : strings) {

   if (s != null && s.length() > 5) {

       result.add(s.toUpperCase());

   }

}

return result;

     */

//    Arays.asList(strings).stream().filter() map()
}
